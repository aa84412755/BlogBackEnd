<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.runner.blog.dao.TouristDao">
    <insert id="addTourist" parameterType="com.runner.blog.entity.Tourist">
        insert into tourist (visitingCities, ipAddress, macAddress)
         values (#{visitingCities},#{ipAddress},#{macAddress})
    </insert>

    <select id="selectip" resultType="int">
        select ipAddress,macAddress from tourist where ipAddress=#{ipAddress} and macAddress=#{macAddress}
    </select>
</mapper>